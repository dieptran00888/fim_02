<%# byebug %>
<%# if @comment.parent && @comment.parent.depth %>
if ($('form').children(('textarea')).val() != "")
{
  $('#list-comment').prepend('<%= j render @comment %>');
  $('form').children(('textarea')).val('');
}
else{
  alert("Can't be blank");
}

<%# end %>
